<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emergency Contact List</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }
        .accordion-button {
            font-weight: bold;
            color: #444;
        }
        .accordion-button:not(.collapsed) {
            background-color: #6c757d;
            color: #fff;
        }
        .accordion-item {
            border: none;
            margin-bottom: 10px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .card-header {
            border-radius: 8px 8px 0 0;
        }
        .contact-details {
            background-color: #f1f1f1;
            padding: 15px;
            border-radius: 0 0 8px 8px;
        }
        .back-button-container {
            text-align: left;
            margin-bottom: 30px;
        }
        .back-button {
            font-size: 16px;
            color: #6c757d;
        }
        .back-button i {
            margin-right: 5px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="back-button-container">
            <a href="/dashboards/student_dashboard" class="back-button">
              <i class="fas fa-arrow-left"></i> Back to Student Dashboard
            </a>
          </div>
        <h1 class="text-center mb-4">Emergency Contact List</h1>
        <div class="accordion" id="contactAccordion">
            <% contacts.forEach((contact, index) => { %>
                <div class="accordion-item">
                    <h2 class="accordion-header" id="heading<%= index %>">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<%= index %>" aria-expanded="false" aria-controls="collapse<%= index %>">
                            <%= contact.role %>
                        </button>
                    </h2>
                    <div id="collapse<%= index %>" class="accordion-collapse collapse" aria-labelledby="heading<%= index %>" data-bs-parent="#contactAccordion">
                        <div class="accordion-body contact-details">
                            <p><strong>Address: </Address>:</strong> <%= contact.address %></p>
                            <p><strong>Email:</strong> <%= contact.email %></p>
                            <p><strong>Office:</strong> <%= contact.office %></p>
                            <p><strong>Fax:</strong> <%= contact.fax %></p>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
